import 'package:flutter/material.dart';

class StoriesCollectedScreen extends StatelessWidget {
  const StoriesCollectedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Achievements Screen"),
      ),
    );
  }
}